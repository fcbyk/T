## demo1

### 什么是正则表达式
正则表达式 = 描述字符串规则的工具

### 正则表达式完整构成
普通字符 + 元字符 + 量词 + 零宽断言 + 修饰符

### 普通字符
> 纯文本，原样匹配

在正则表达式中，大多数字符都是普通字符，它们会原样匹配自身

### 元字符
在正则表达式中具有特殊语法含义的符号<br/>
不再代表自己本身的字面意思，是正则表达式的「命令 / 语法」

### 总结
- 普通字符：老老实实匹配自己
- 元字符：干特殊活儿，不匹配自己