## demo2

### 量词
控制前面紧邻的原子 / 字符 / 分组出现多少次

- `+` 至少 1 次
- `*` 任意次 (0 次及以上)
- `?` 0 次或 1 次
- `{n}` 固定 n 次
- `{n,}` 至少 n 次
- `{n,m}` n 到 m 次


### 贪婪 vs 非贪婪
- 默认量词都是贪婪匹配：尽可能多拿在
- 量词后面加 `?` 变成非贪婪(懒惰)：尽可能少拿
- 贪婪 `+` , `*` , `?` , `{m,n}`
- 非贪婪 `+?` , `*?` , `??` , `{m,n}?`