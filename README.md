# demo 34

## 组件事件

- **父组件**：
  定义一个「回调函数」，并通过 `@自定义事件名` 绑定、暴露给子组件。

- **子组件**：
  通过 `emit('事件名', 数据)`，**主动调用父组件传过来的这个回调**，还能顺便传参数。

- 因为是**子调用父的函数**：
  - 子**不能直接改父的数据**
  - 只能调用父的函数，**让父自己在函数里改自己的数据**